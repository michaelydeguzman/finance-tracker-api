<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Intentionally empty. Domain references no projects and no packages — that is the
    layering rule, not an oversight — and TargetFramework, Nullable and ImplicitUsings
    are inherited from Directory.Build.props at the repo root.
  -->

</Project>
